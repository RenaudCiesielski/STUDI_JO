@model List<Category>

<h1>Catégories Tickets</h1>
<table class="table table-bordered table-striped">
    <thead>
    <tr>
        <th>
            Catégorie ticket
        </th>
        <th>
            Description
        </th>
        <th>
            Commander
        </th>
        </tr>
    </thead>
    <tbody>
        @foreach (var obj in Model)
        {
            <tr>
                <td>@obj.Name</td>
                <td>@obj.Description</td>
                <td>@obj.DisplayOrder</td>
            </tr>
        }
    </tbody>
</table>